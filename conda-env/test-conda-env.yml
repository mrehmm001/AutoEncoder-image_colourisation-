# Create new environment:
# `conda env create -f test-conda-env.yml`
#
# Then activate:
# `conda activate test`

name: test
dependencies:
  - python=3.9
  - numpy
  - pandas
  - tensorflow_io
  - opencv-python
  - scikit-image
  - matplotlib
  # Installing packages directly with pip.
  # - pip
  # - pip:
  #   - some-random-non-anaconda-package
  - scikit-learn
  - scikit-learn-intelex
  - scipy
  - tensorflow-gpu
  # Specifying packages from other repositories.
  # - conda-forge::librosa
