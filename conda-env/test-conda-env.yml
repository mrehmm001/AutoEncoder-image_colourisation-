# Create new environment:
# `conda env create -f test-conda-env.yml`
#
# Then activate:
# `conda activate test`

name: test
dependencies:
  - python=3.9
  - numpy
  - pandas
  - matplotlib
  # Installing packages directly with pip.
  # - pip
  - pip:
    - tensorflow_io
    - opencv-python
    - scikit-image
  - scikit-learn
  - scikit-learn-intelex
  - scipy
  - tensorflow-gpu
  # Specifying packages from other repositories.
  # - conda-forge::librosa
